import 'package:flutter/material.dart';

class GridCourses {
  final String title;
  final IconData icon;
  final bool isAll;
  final Color circleColor;

  GridCourses({
    required this.title,
    required this.icon,
    required this.isAll,
    required this.circleColor,
  });
}
